PORT=3003

# Neon Postgres (https://neon.tech)
# Connection string from Neon dashboard → Connection details
DATABASE_URL=

# JWT secret for auth (generate a random string in prod)
JWT_SECRET=change-me-in-production

# Meta / Facebook App (create at https://developers.facebook.com)
# For testing: use a User Access Token from Graph API Explorer
# with ads_management, ads_read, business_management permissions
META_APP_ID=
META_APP_SECRET=

# For CLI sync + Refresh button: User Access Token from Graph API Explorer
META_ACCESS_TOKEN=
