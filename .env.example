# En local : le proxy Vite envoie /api vers localhost:3001 (serveur du dashboard). Lance le serveur : cd server && npm run dev
# En prod : URL du backend + /api (ex: https://reporting-api-xxx.vercel.app/api)
# Pour pointer vers reporting-api en local : VITE_PROXY_TARGET=http://localhost:3003 (dans .env à la racine)
VITE_API_URL=
